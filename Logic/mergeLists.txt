Se tienen 2 listas de Nodos 
Se crea el result y el auxiliar
Se va ciclando entre todos los list1 y list2 nodes
Se verifica que no sea nullptr el ->next 
Se usa el aux, se compara list1(n) con list2(n), si list1(n) < list2(n) se sigue avanzando en list1 hasta que list2(n) sea menor que un valor entonces se avanza en list2(n) y asi
